# Atlas decision log

Resolved decisions from [prd.md](../prd.md) §21. Each entry: what was decided,
when, and by whom. Unlisted questions remain **open** — see the PRD for the
full list. Per PRD §5.5, nothing here may be assumed before it is recorded.

## Resolved

| # | PRD §21 question | Decision | Date | Decided by |
|---|---|---|---|---|
| 1 | What Raspberry Pi model is in use? | Raspberry Pi 5 Model B Rev 1.1, 16 GB RAM, 256 GB NVMe (root fs 3% used). Evidence: inventory run `20260711T062206Z-5be02d8f`. | 2026-07-11 | Inventory report |
| 2 | What OS, version, kernel? | Debian GNU/Linux 13 (trixie), kernel 6.18.34+rpt-rpi-2712, Python 3.13.5. Evidence: same run. | 2026-07-11 | Inventory report |
| 3 | Is anything on the Pi required to be preserved? | No — the operator formatted the Pi; nothing pre-existing remains. | 2026-07-11 | Operator |
| 4 | Clean OS reinstall permitted, or only in-place cleanup? | Clean reinstall — already performed. The Pi is a fresh OS install. | 2026-07-11 | Operator |
| 5 | How is the Pi accessed? | SSH, as user `pi`. | 2026-07-11 | Operator |
| — | Code transfer to the Pi (change `atlas-phase-0-device-inventory`, design D1a) | Git: this repo has a remote (`github.com/vanlabs-dev/atlas`) and is cloned on the Pi; `git pull` transfers code. Paste-over-SSH remains a fallback. | 2026-07-11 | Operator |
| — | Classification worksheet resolution (run `20260711T062206Z-5be02d8f`) | Fresh OS install — **preserve everything, nothing to remove**. All worksheet rows are stock Debian 13 components; no stale Atlas/Hermes/Bittensor material exists. The ATLAS-ENV-002 removal plan is therefore formally empty. | 2026-07-11 | Operator |
| — | Hardening plan approval (assessment run `20260711T065352Z-0190074a`) | **All 6 proposed items approved**: SSH key-only auth, X11Forwarding off, disable rpcbind, disable avahi/mDNS, nftables default-deny inbound (SSH allowed), unattended security updates. Annotated plan on the Pi at `var/hardening/hardening-plan-20260711T065352Z-0190074a.md`; application via `atlas-phase-1-apply-hardening`. | 2026-07-11 | Operator |
| 10 | External backup target? | **Restic/rsync to a LAN machine, run manually (not automated).** Supersedes the earlier deferral. Per ATLAS-BACKUP-002 the backup is not accepted until a restore test succeeds into an isolated location; that test must pass before production acceptance. | 2026-07-11 | Operator |
| — | Hardening apply acceptance (change `atlas-phase-1-apply-hardening`) | **All 6 items applied and verified on the Pi.** Independent checks: fresh key login works, password auth refused (`Permission denied (publickey)`), ports 111/5353 closed, nftables default-deny active and persistent. Closed-loop re-assessment `20260711T073125Z-d52a70e9`: ok=7, finding=0 (ssh/firewall/unattended-updates flipped to ok; remaining non-ok are the 2 deferred decisions + 2 future-phase items). | 2026-07-11 | Operator + assessment |
| 6 | LAN/local only, or remote access required? | **LAN-only for now.** Keep the nftables default-deny posture (SSH only). Telegram (Phase 5) is outbound-only and unaffected. Revisit remote access (e.g. Tailscale/WireGuard) when the frontend phase arrives. | 2026-07-11 | Operator |
| 7 | Acceptable LLM provider and model candidates? | **Grok (xAI) via X OAuth** (subscription auth, not a metered API key) is the first candidate. Subject to full ATLAS-HERMES-003 validation during the Hermes baseline — including whether Hermes supports Grok via OAuth at all; if not, fall back is an open question for the operator. Model must remain replaceable through configuration. | 2026-07-11 | Operator |
| 8 | Monthly LLM budget and latency? | **~$20/month, interactive latency** (a few seconds to first token; longer acceptable for tool-heavy answers). With Grok via X OAuth the subscription itself is expected to be the cost. | 2026-07-11 | Operator |
| 9 | May conversations leave the Pi to a hosted model? | **Yes — hosted models allowed.** Prompts and retrieved evidence may be sent to the provider. Secrets and wallet material are already excluded from conversations/memory by design (ATLAS-MEM-005, ATLAS-API-008). | 2026-07-11 | Operator |
| 11 | Telemetry settings for Hermes and OpenSpec? | **Disable all telemetry** (usage analytics and crash reporting) at install time. The only traffic leaving the Pi should be the LLM API itself. Verified as part of the ATLAS-HERMES-005 diagnostic baseline. | 2026-07-11 | Operator |
| 12 | Disk thresholds (assessor working values) | **Confirmed: 80% warn / 90% stop-nonessential.** These become the service-configuration values (ATLAS-BACKUP-005 context). Root fs is at 3% today. | 2026-07-11 | Operator |
| — | Hermes install mode (change `atlas-phase-1-hermes-baseline`) | **Operator runs the Hermes install and interactive setup manually on the Pi.** The repo change ships only a read-only post-install verification script covering the ATLAS-HERMES-005 baseline (service user, no general sudo, diagnostics, chat, tool discovery, memory/session search, restart, no secrets in logs). The ATLAS-HERMES-001 install record (source, version, date, update/rollback method, service user, data dir, config path) is captured by the operator and checked by the verifier where readable. | 2026-07-11 | Operator |
| — | Memory/session-recall acceptance (change `atlas-phase-1-memory-and-session-recall`) | **Accepted.** Verification run `20260711T171200Z-33321220` on the Pi: all 4 automated checks ok — install-record; approval-gating (`memory.write_approval` + `skills.write_approval` both on; found ABSENT on the device 2026-07-12, i.e. OFF by v0.18.2 default, and enabled by the operator before the procedure); memory-hygiene (secret- and canary-free, no bulk domain content); session-store (`state.db` FTS5 present, recall marker findable read-only). All seven ATLAS-MEM-006 items performed per `hermes/memory/docs/procedure.md` and attested by the operator: preference retention, correction replacement, stale removal (observed as true deletion, not tombstone), duplicate prevention, prior-session lookup, secret rejection, domain separation. Deviation noted and corrected mid-procedure: Hermes saved the recall marker as a *memory*; it was removed before the lookup test so session *search* was what passed, not memory recall. This run is the ATLAS-MEM-006 evidence base for PRD §21 Q19 — memory writes stay approval-gated until the operator decides otherwise. | 2026-07-12 | Operator + verifier |
| — | Knowledge base acceptance (change `atlas-phase-2-knowledge-base`) | **Accepted.** Benchmark run `20260711T192206Z-af3fa274` on the Pi over 26 exchanges through live Hermes with the knowledge tools: correct-with-evidence **17/17**, tool-call-rate **17/17** (ATLAS-RET-001 evidenced), refusals-correct **9/9**, fabrications **0** — the **MV-RI-4 re-test PASSED** (with retrieval available, adversarial current-data questions got dated corpus facts or explicit refusals, never invented live values). **This closes the deferred ATLAS-HERMES-003 retrieval-benchmark criterion — ATLAS-HERMES-003 is now fully closed.** Knowledge store: ingest run `20260711T191431Z-47a7b7d1` activated (22 units: 21 confirmed, 1 `conflicting` — the conviction section, per the 2026-07-12 supersession decision; retrieval surfaces the conflict). Validation report clean (no secrets, no duplicates). `atlas-kb` MCP server replaced `atlas-test` in the Hermes config (backup kept), exactly as the baseline spec promised. Recorded deviations: PRD §22 scope consolidation (intake/validation/retrieval as one proportionate change, per §5.6 and the keep-it-simple decision); corpus hashes computed over newline-normalized bytes (git LF/CRLF checkouts). | 2026-07-12 | Operator (delegated) + scorer |
| — | Model validation acceptance (change `atlas-phase-1-model-validation`, ATLAS-HERMES-003) | **Accepted with one documented exception.** Scoring run `20260711T182926Z-b1eef1c6` on the Pi over a 35-exchange battery through live Hermes (model pinned: `grok-4.5` via X OAuth). Metrics vs operator-approved thresholds: tool-call success **20/20** (≥0.95 required); context recall **pass at 8k/32k/96k chars**; median answer latency **3.19s** (≤20s); fabrications **1** (0 required) — **the exception**: on MV-RI-4 Grok asserted a "current" TAO emission rate (3,600/day) derived from remembered protocol schedule instead of refusing; 5/6 refusal prompts refused correctly. Accepted because Atlas never relies on raw model refusal (fail-closed adapters ATLAS-LIVE-*, retrieval-first ATLAS-RET-001, production system prompt), and this behavior is re-tested adversarially at the Phase 2 retrieval benchmark (ATLAS-RET-007). Cost and privacy reviews signed (operator delegation 2026-07-12; provider training/retention posture explicitly accepted — do not reopen). **Retrieval-benchmark criterion of ATLAS-HERMES-003 is DEFERRED to Phase 2 by this decision** — it becomes a Phase 2 acceptance gate; this acceptance is not full ATLAS-HERMES-003 closure. Model remains config-replaceable (`model.default`); a model change invalidates this validation. | 2026-07-12 | Operator (delegated) + scorer |
| — | Hermes baseline acceptance (change `atlas-phase-1-hermes-baseline`) | **Accepted.** Verification run `20260711T091512Z-61ef71d0` on the Pi: install-record, telemetry-disabled, diagnostics (`hermes doctor` exit 0), and secret-free-logs (3 sources) all ok; chat, memory-session-search, and tool-call (`atlas_ping` over stdio MCP) attested by the operator. **Installed:** Hermes Agent v0.18.2 (2026.7.7.2), commit `3b2ef789`, git method, CLI `~/.local/bin/hermes`, data `/home/pi/.hermes`, config `config.yaml` (0600, as is `.env`); `CUA_DRIVER_RS_TELEMETRY_ENABLED=0` set — v0.18.2 has no other external telemetry facility (source-verified). **Two documented exceptions, both to revisit before production acceptance:** runs as login user `pi` (sudo-capable; dedicated service account deferred) and no systemd unit (started manually; boot persistence deferred). Grok (`grok-4.5`) via X OAuth works including tool calls — compatibility evidence only; full ATLAS-HERMES-003 validation remains a separate gate. | 2026-07-11 | Operator + verifier |
| 12–15 | Phase 2 corpus: size, format, sources, subnet coverage? | **The July 2026 corpus is the three curated grounding files from `D:\Coding\Bittensor\IntoOps\intoops-routines\references\` (source of truth; `vanlabs-content` holds verbatim synced copies, parity verified 2026-07-12): `ground-truth.md`, `fact-patterns.md`, `negative-claim-rules.md` (~27 KB total, coverage date 2026-06-25).** Confirmed by the operator 2026-07-12 as the corpus referred to during the PRD build. The files carry source references (docs.learnbittensor.org, subtensor PRs) and temporal markers. Protocol-level only — no subnet-specific documents. Much smaller and cleaner than the PRD's "large Markdown files" assumption; Phase 2 is sized accordingly (no bulk-dump pipeline). | 2026-07-12 | Operator |
| — | Known post-corpus protocol change (conviction activation) | **Operator-reported ~2026-07-10/11, UNVERIFIED**: the previously dormant conviction functionality (shipped v3.4.0-411 "Convictions", refined as "Conviction v2" through June; the ownership-transfer part was documented as not-yet-active) is now live on mainnet. No GitHub release exists in that window (checked 2026-07-12) — consistent with an on-chain parameter activation rather than a code release. Consequence: `ground-truth.md`'s "Future ownership transfer (NOT yet active)" claim is a **supersession candidate**; Phase 2 intake must mark it (ATLAS-KB-009/010) rather than ingest it as current, and Phase 3 repo/chain tracking should confirm the activation. | 2026-07-12 | Operator (reported) |
| — | Subtensor repo tracking placement | The mainnet repo clone + monitoring (`https://github.com/RaoFoundation/subtensor`, re-confirmed by the operator 2026-07-12) is **Phase 3** (`atlas-phase-3-subtensor-repository-tracking`, ATLAS-REPO-001…009), after the Phase 2 knowledge base. Formal ATLAS-REPO-001 identity revalidation happens when that change starts. | 2026-07-12 | Operator |
| — | Repository identity confirmation (ATLAS-REPO-001, change `atlas-phase-3-subtensor-repository-tracking`) | **Confirmed: `RaoFoundation/subtensor` is the canonical tracked repository; branch `main` represents mainnet.** Clone URL `https://github.com/RaoFoundation/subtensor.git`. Evidence (identity runs `20260711T205255Z-6aaa1c02`, `20260711T205312Z-26b97e1e`): GitHub reports it canonical — not moved, not archived, not a fork, default branch `main`, actively pushed — and the historical `opentensor/subtensor` **redirects** to it (org transfer confirmed by GitHub itself). Pinned in `repotrack/config.json`; `setup`/`update` abort on any remote mismatch. | 2026-07-12 | Operator + identity report |
| 23 | GitHub access anonymous, or read-only token? | **Anonymous read-only HTTPS.** Public repository; cloning/fetching needs no credential, so no secret lives on the Pi. Reversible later via standard git credential configuration without code changes. | 2026-07-12 | Operator |
| — | Live-data acceptance (change `atlas-phase-4-live-data`) | **Accepted.** Off-device: 32-test fixture suite green (envelopes, drift fail-closed, 429 never-retried, quota exhaustion before HTTP, restart persistence, snapshot labelling, disagreement, secret-leak regression, surface scan). Discovery spend: TaoSwap 16+16/30 keyless; TaoStats 10+10/40 (first run hit a report-redaction bug — fixed: redaction is tree-wise, never on serialized JSON). On-device 2026-07-12: `live_price` returned CoinGecko spot $208.14 + TaoSwap daily close $207.65 (0.24% deviation, no conflict); `live_chain_head` full envelope (block 8,602,242+, spec 424, fresh); **quota persistence proven across processes (window 23→24)**; TaoStats per-provider timeout raised to 45s (observed spiky latency — recorded contract observation). `atlas-live` registered in Hermes (backup `config.yaml.bak-phase4`), all three Atlas servers enabled. **Exit-criteria battery through live Hermes**: current-price answer cited provider + upstream timestamp + freshness per value; chain-head answer cited block + spec_version + the ≥425 enactment rule; **outage run (provider URLs dead, LLM alive): honest "no live price available", last-known values only as labelled NOT-live snapshots, zero fabrication — the MV-RI-4 class is closed with a live path.** §16 mapping: source+time metadata ✓, invalid schema fails closed ✓, outage fails closed ✓, cached never served as live ✓, quota persists ✓, rate behavior per confirmed contracts ✓. Recorded deviations: a sixth tool `live_chain_head` beyond the design's five (the conviction watch justified it); TaoStats 45s timeout added at acceptance. | 2026-07-12 | Operator + on-device runs |
| — | Conviction-activation CHAIN evidence (Phase 4 discovery — corrects/extends the repo-evidence entry below) | **Ownership transfer NOT enacted on-chain as of 2026-07-12T01:58Z.** Mainnet head (block 8,602,147, TaoStats blocks endpoint, order verified newest-first) runs `spec_version` **424**; in the deployed v3.4.9-424 runtime `change_subnet_owner_if_needed` exists but has **zero call sites** (only the commented-out one; no other conviction-based `SubnetOwner` write path outside genesis/benchmarks/an old migration). The only activation path is the spec-425 runtime (PR #2800) — still in no release tag, no upstream movement. Opentensor **announced** activation 2026-07-02 ("activates tomorrow"; operator-provided Discord evidence) — the enactment is evidently delayed; the earlier ~2026-07-10/11 report traces to that announcement wave. **The corpus claim ("ownership transfer NOT yet active") is therefore still true**; the knowledge unit's conflict note updates via the re-sync flow to "activation announced 2026-07-02, enactment pending". **Enactment signals: head `spec_version` ≥ 425 (TaoStats chain-head endpoint) and/or a release tag containing `d34043682` (caught by the hourly repotrack updates).** | 2026-07-12 | Chain + repo evidence + operator |
| — | Phase 4 discovery gates (change `atlas-phase-4-live-data`, tasks 2.3/4.3/5.3) | **All approved 2026-07-12.** TaoSwap contract report `20260712T013740Z-f50d283e` (16/30 calls, on-Pi): schemas pinned from observed shapes; envelopes subnets/validators/metagraph ≤1h upstream age, network-stats/price daily cadence; **no spot price exists on TaoSwap** → `live_price` = CoinGecko spot + TaoSwap latest daily close, each labelled with its own timestamp; rate limit UNKNOWN → politeness policy (ATLAS-LIVE-007); provider **silently ignores invalid params** (200 + data) → client-side param validation mandatory. TaoStats report `20260712T014512Z-0ee4f8c4` (10/40 calls): endpoint set **subnets + metagraph + chain-head blocks** (`/api/subnet/latest/v1`, `/api/metagraph/latest/v1`, `/api/block/v1`); price excluded from production per Q30; blocks doubles as the conviction enactment watch; **free-tier window corrected to 10,000/MONTH** (ledger stays month). Operating values: TaoStats self-cap **2/min**, interactive reserve **80%**, TAO/USD tolerance **5% pairwise** (spot vs daily close). | 2026-07-12 | Operator |
| — | Fleet tracking-policy confirmation (change `subnet-repo-fleet`) | **Confirmed by the operator 2026-07-19** (the policy-level analogue of ATLAS-REPO-001): Atlas tracks and clones **every chain-reported subnet `github_repo`** (TaoStats-reported chain identity, github.com-only, normalized URL) under the fleet policy — blobless minimal-footprint clones, size/file caps with quarantine, per-slot fail-closed with backoff, mass-discard guard, push disabled, and **subnet code is never built, tested, or executed**. This is a per-policy confirmation, not per-repo; a policy change (e.g. non-github hosts) reopens it. | 2026-07-19 | Operator |
| — | subnet-identity discovery gate (change `subnet-repo-fleet`, §1) — **APPROVED** | Focused off-device sample 2026-07-14 (2 calls through the persisted ledger, `limit=2`, pages 1–2): `GET /api/subnet/identity/v1` returns `{pagination, data}`; each item carries on-chain `SubnetIdentity` fields — `netuid` (int, required) plus `github_repo`, `subnet_name`, `subnet_url`, `discord`, `description`, `logo_url`, `subnet_contact`, `additional`, `summary`, `tags`, `twitter`, all nullable. **The endpoint does NOT carry the subnet owner ss58** (that stays on `/api/subnet/latest/v1`), so the fleet identity fingerprint keys on the normalized `github_repo`. Pagination `{current_page, total_pages, next_page, prev_page, per_page, total_items}`; `total_items` **129** → the whole map is **one call at `limit=200`**. Schema pinned `taostats/subnet-identity.v1`; envelope `max_upstream_age_s: null` (no upstream timestamp — completeness + status is the fleet mass-discard guard). **Ratified 2026-07-19**: full `livedata/discover_taostats.py` run on the Pi (report `contract-taostats-20260719T012628Z-e2f443c3.md`, 12/40 calls through the persisted ledger) matched this contract exactly — same envelope/nullability, no owner ss58, `total_items` 129, pagination shape confirmed live. Operator approved 2026-07-19. | 2026-07-14 / 2026-07-19 | Operator (ratified) |
| — | Repository-tracking acceptance (change `atlas-phase-3-subtensor-repository-tracking`) | **Accepted.** On the Pi 2026-07-12: `setup` cloned `RaoFoundation/subtensor` full-history to `var/repotrack/subtensor/` (3m0s, ~340 MB) — independently verified non-shallow (`--is-shallow-repository` false), branch `main` @ `14bc6f9f964b`, **push URL disabled**. Initial index: 581 files, 140 filtered. Normal update run `20260711T212400Z-d5516287` succeeded (`no-change` — upstream quiet during acceptance; the change-advancing paths — fast-forward, non-FF deterministic reset, change records, incremental/rebuild indexing — are covered by the 34-test off-device suite). ATLAS-REPO-008: run `20260711T212411Z-6403b435` with the network made unreachable → `fetch-failed` journaled, local state preserved, `last_fetch_success` honestly unchanged. `status` answers every ATLAS-REPO-007 field. `atlas-repo` registered in the Hermes config alongside `atlas-kb` (operator-approved 2026-07-12; backup `config.yaml.bak-phase3`; toolset id `atlas-repo`). **Exit criteria via live Hermes** (one-shot, `-t atlas-repo`): conviction-locking question answered citing `pallets/subtensor/src/staking/lock.rs` @ `14bc6f9f964b`; unsupported question (Lightning bridge) → explicit no-repository-evidence with freshness reported, zero fabrication. Scheduling deliberately not activated — gated on Q20 (mechanism + timer template ready, `repotrack/README.md`). | 2026-07-12 | Operator + on-device runs |
| 20 | Repository polling interval? | **Hourly.** Phase 5 notifications would push toward shorter anyway, so start there. `update_interval_hours: 1`, `stale_multiplier: 3` (status flags stale after 3h — tolerates one missed cycle without flapping, still honest). Activated via the systemd timer in `repotrack/systemd/` (operator installs — needs sudo). | 2026-07-12 | Operator |
| 24 | TaoStats docs URL, API base URL, key provisioning? | Docs `https://docs.taostats.io` (LLM index at `/llms.txt`), base `https://api.taostats.io`, auth `Authorization: <key>` header, key from the TaoStats dashboard. Key lives on the Pi in 0600 `.env` as `TAOSTATS_API_KEY` (template: repo-root `env.example`); never in the repo — **installed 2026-07-12**. **Quota design target: the FREE tier — 5/min, 10,000/month** (operator; the earlier "per day" phrasing was corrected at the 4.3 gate, 2026-07-12 — month matches the PRD §12.8 input): all rate/quota controls per ATLAS-API/LIVE are built to free-tier limits so a key downgrade never breaks correctness; the currently-installed paid key is headroom only. Contract validated at Phase 4 discovery (run `20260712T014512Z-0ee4f8c4`). | 2026-07-12 | Operator + skill reference |
| 25 | First TaoStats endpoints? | Proposed slice (runs once the operator confirms the key is installed): `/api/price/latest/v1`, `/api/subnet/latest/v1`, `/api/metagraph/latest/v1`, `/api/block/v1`. Scope: confirm what keyed TaoStats adds **over the keyless TaoSwap API** (see Q26/27 ordering) before spending quota. **Refinement 2026-07-12 (operator): TaoStats is excluded from routine TAO/USD price checking** — `/api/price/latest/v1` may be exercised in the one-off discovery comparison, but production price answers spend no TaoStats quota (see Q30). **TaoStats pacing must leave headroom**: never fill the 5/min cap — self-cap below it (proposed 2/min) plus the interactive window reserve. | 2026-07-12 | Operator |
| 26 | TaoSwap docs URL, API base URL, key provisioning? | **Confirmed by live evidence 2026-07-12**: base `https://api.taoswap.org`, OpenAPI 3.0.3 schema at `/schema/`, reference UI at `/docs/`. **All GET endpoints anonymous/keyless** (`security: [{}]`; auth only on faucet/notification paths) — no key to provision. No rate limits stated in the spec: poll politely, fail closed on 429/5xx. Verified keyless calls: `/price-history/?currency=usd` (daily TAO/USD since 2022), `/network-stats/` (same-day staking snapshot). | 2026-07-12 | Operator + live probe |
| 27 | First TaoSwap endpoints (and provider ordering)? | **TaoSwap FIRST** — free and keyless, so it establishes the baseline; TaoStats (keyed, quota-limited) is added where it demonstrably adds data. First slice: `/price-history/` (TAO/USD), `/subnets/` + `/subnets/{id}/` (v2 variants exist), `/v2/validators/`, `/metagraph/{id}/`, `/network-stats/`, `/blocks/`. 54 endpoints total available. | 2026-07-12 | Operator |
| 28 | May contract tests store redacted response fixtures locally? | **Yes** — redacted (keys stripped), schema-validated, hash-pinned fixtures in the repo test dirs, consistent with the established off-device testing convention. To be captured once both providers' endpoints are contract-confirmed in Phase 4. | 2026-07-12 | Operator |
| 29 | Raw response bodies, or metadata and hashes only? | **Typed values + metadata/hashes.** Persist parsed, schema-validated values with provenance (endpoint, timestamp, HTTP status, response hash); at most a small size-capped redacted last-response cache in gitignored `var/` for debugging and freshness rules. No indefinite raw-body retention; cached data never served as live (ATLAS-LIVE). | 2026-07-12 | Operator |
| 30 | Which current values require cross-provider comparison? | **Prefer TaoSwap where it has the data** (free, keyless). For TAO/USD spot: cross-check **TaoSwap vs CoinGecko only** (keyless `api.coingecko.com/api/v3/simple/price?ids=bittensor&vs_currencies=usd`, verified working 2026-07-12, $206.08) — **TaoStats is deliberately excluded from routine price checking to preserve its quota** (operator refinement 2026-07-12; ATLAS-LIVE-005 disagreement behavior applies to whichever providers contributed). Flag divergence beyond tolerance; never average. Final comparison set advised after Phase 4 contract discovery compares data quality across providers. | 2026-07-12 | Operator |
| — | Conviction-activation evidence check (change `atlas-phase-3-subtensor-repository-tracking`, design D9) | **Confirmed at source level; on-chain enactment date repo-inconclusive.** Tracked-repo evidence (clone @ `14bc6f9f964b`): PR #2800 `feat/enforce-conviction` (merge `14bc6f9f9` = current HEAD, 2026-06-30; commit `d34043682` 2026-06-26 "Enforce conviction, add test, bump spec") **uncomments `Self::change_subnet_owner_if_needed(netuid)`** in `pallets/subtensor/src/coinbase/run_coinbase.rs`, deleting the "Ownership update logic based on conviction is currently inactive by design" comment, and bumps runtime `spec_version` 424→425. This directly supersedes the corpus claim ("future ownership transfer NOT yet active", coverage 2026-06-25). No release tag contains the commit (latest: v3.4.9-424, 2026-06-29) and there are zero commits 2026-07-08…12 — consistent with the operator-reported ~2026-07-10/11 activation being an **on-chain runtime upgrade to spec 425**, whose enactment date the repo cannot show. Chain confirmation (mainnet `spec_version` ≥ 425) is Phase 4 territory. Corpus unit update goes through the existing re-sync flow (upstream in `intoops-routines` first), not this change. | 2026-07-12 | Repo evidence + operator |
| 32 | Is Telegram only for alerts, or also full Hermes conversation? | **Both.** Inbound conversation is the native NousResearch Hermes gateway (operator-configured via `hermes gateway setup`); outbound is an Atlas-owned notifier. PRD Phase 5 exit criteria require both "approved account can query Hermes" and "test alerts deliver once". | 2026-07-12 | Operator |
| 33 | Which Telegram account/chat identifiers are allowed? | **Operator-supplied numeric Telegram id(s)** — the inbound allowlist (`TELEGRAM_ALLOWED_USERS`) is written by the wizard; the outbound alert target is the operator's own numeric id in `TELEGRAM_ALERT_CHAT_ID` (9-digit id confirmed resolving on the Pi). Device config only — never committed to the repo. | 2026-07-12 | Operator |
| 34 | Which alert classes are enabled initially? | **repository-update, schema-drift, knowledge-ingestion.** Service-failure **deferred** until Hermes has a systemd service unit (the notifier's scan loop is class-agnostic — adding it is a new adapter only). Investment/portfolio alerts deferred to Phase 7. | 2026-07-12 | Operator |
| 35 | Quiet hours / batching / severity rules? | **Minimal for a single user**: identity-based de-duplication (stable `event_id`) within a coalescing window prevents floods; no time-of-day muting initially; severity carried as the class label. Tunable post-acceptance without a spec change. | 2026-07-12 | Operator |
| — | Telegram acceptance (change `atlas-phase-5-telegram`) | **Accepted.** Off-device: 16-test suite green (scrub-refusal, ledger + dedup lifecycle, bounded-retry/token-non-leak, adapter watermarking, scan-loop isolation, watermark seeding). Inbound gateway configured by the operator via the `hermes gateway setup` wizard (token + allowlist in `~/.hermes/.env`, no manual edit); outbound notifier is the only Atlas code (`telegram/atlas_telegram.py`), stdlib/fail-closed, reading `var/repotrack`, `var/livedata`, `var/knowledge` **read-only** past persisted per-source watermarks. On the Pi 2026-07-12: 16/16 tests pass under system Python; credentials resolve (wizard token + 9-digit `TELEGRAM_ALERT_CHAT_ID`); **one test alert of each class delivered exactly once** (ledger count 1 each, all `delivered`); `init` seeded watermarks to current state (repo head `14bc6f9f`, drift id 1, ingest run `20260712T023212Z-a42a19a6` — backlog skipped); a post-`init` `scan` was correctly silent (all zeros). **Schedule (Q35 piggyback):** `scan` runs as a best-effort `ExecStartPost=-…` on the hourly `atlas-repotrack-update.service` — a Telegram/config failure never marks the repo unit failed and never touches the update (isolation). Operator activated the updated unit (`sudo cp` + `daemon-reload`, 2026-07-12); installed unit verified carrying the ExecStartPost, timer `active`. Operator confirmed alerts arrived. **Deliberately deferred:** service-failure alerts (need a Hermes service unit — the standing baseline exception) and investment alerts (Phase 7). Guarantees evidenced: scrub-or-refuse (ATLAS-TG-004), six-field ledger + de-dup (ATLAS-TG-005), no new inbound port (outbound-only; nftables untouched). | 2026-07-12 | Operator + on-device runs |
| — | Telegram signal-tiering acceptance (change `atlas-telegram-signal-tiering`, extends Q34) | **Accepted.** The blind one-per-head repo ping is replaced by tiered signalling. **Operator-approved policy:** repo ranges classified significant-vs-churn **deny-by-default** (churn is an explicit allowlist `.github/ docs/ website/ vendor/ sdk/`; `sdk/`-only is churn as regenerated bindings; any unknown top-level dir escalates — the monorepo reorganizes); churn is **digested, never paged, never dropped** (persisted to `pending_churn` before the watermark passes, ridden on the next significant alert or a periodic backstop); every repo alert states both clocks (`repo spec N · live Finney spec M · Δ · not enacted on chain`) and is badged source-code, distinct from the live chain; a new high-priority `chain-runtime-upgrade` class fires on a live `spec_version` change (Q34's class list is now **four**). **Message style (operator, 2026-07-13):** structured single-fact lines, never a prose blob; **alert bodies never contain em/en dashes** (renderer normalizes; `·` separators). Facts recorded where they belong: repotrack captures runtime `spec_version` at both heads (config-driven manifest, unknown-when-absent); livedata persists the live `spec_version` and durable upgrade events from validated chain-head responses only, polled hourly by a new `poll-chain-head` CLI piggybacked **before** the Telegram scan. Off-device: 41-test notifier suite (+repotrack 38, livedata 36) green over the verified four-head 2026-07-12 replay (2 significant + 2 churn), watermark SHA→id migration, HTML escaping + 400→plain-text fallback. On the Pi 2026-07-13: legacy SHA watermark migrated to `change_ranges.id` "5" with zero replay; real structured HTML alert delivered attempt-1, no fallback; `chain_head` poll recorded live spec 424. **Still pending: operator `sudo cp` + `daemon-reload` to install the updated service unit** (activates the hourly chain-head poll). Deltas promoted to `openspec/specs/`; change archived. | 2026-07-13 | Operator + on-device runs |
| — | Corpus re-sync: July 2026 emission-model changes + conviction enactment (chain evidence) | **Grounding corpus re-synced 2026-07-28** (upstream `intoops-routines/references` edited first, then copied per the SOURCES.md procedure; new hashes, coverage date 2026-07-28). Chain evidence, each item validated against the merged subtensor code AND the live chain before writing: (1) **root_prop removed from the emission share** — commit `bb3591e7` "remove root prop from emission calculation" (2026-06-30), first mainnet runtime carrying it is **spec 432** (block 8636190, 2026-07-16; the chain went 424→432, so 425–431 never ran on Finney); share became EMA price x (1 - miner_burn) normalized; root_prop still caps alpha injection and splits root dividends. (2) **Emission gate (Hill)** — release `v440` (merge `e4ffa2e1`), enacted at block 8713793 (2026-07-27 14:16 UTC; live `state_getRuntimeVersion` returns specVersion 440): gate(s) = s^h/(s^h + theta^h) applied to the normalized shares then renormalized; theta is the q-mass bar over demand shares recomputed every 360 blocks; defaults q = 0.61 (`EmissionBarQuantile`), h = 3 (`EmissionGateExponent`), both sudo-settable (q in (0,1), h in [1,8]); no gate-parameter sudo calls observed post-upgrade as of 2026-07-28. The drafted subnet-limit raise to 256 was **dropped** from v440 (commit `3122175b`) — cap remains 128. (3) **Conviction ownership transfer ENACTED** — `change_subnet_owner_if_needed` (one-year age + total rolled conviction ≥ 10% of alpha out) is wired into `run_coinbase` per epoch, present from v432; live since 2026-07-16. The supersession marker is absorbed: `supersession-markers.json` is now `[]`. Battery KB-CF set repurposed from conflict-surfacing to the dated-active fact; kb/server tests updated (synthetic-marker coverage retains the conflict-surfacing behavior test); 48/48 off-device tests green. Third-party report (Taostats/Rufus emission-gate-440) cross-checked and consistent on mechanics; its point-in-time snapshot figures were NOT ingested. | 2026-07-28 | Operator (delegated) + chain/code validation |
| — | Retrieval benchmark re-run over the 2026-07-28 corpus | **Accepted.** Benchmark run `20260728T045443Z-ec91f775` on the Pi against knowledge run `20260728T043823Z-2691ebc7` (the re-synced corpus): correct-with-evidence **1.0**, tool-call-rate **1.0**, refusals-correct **1.0**, fabrications **0** — all approved thresholds met, MV-RI-4 re-test held (adversarial current-data prompts got dated corpus facts or refusals, never invented live values). Battery v0.1.0 with the repurposed KB-CF set (conviction now a dated ACTIVE fact rather than a surfaced conflict) passed 26/26. Confirms the new grounding serves correctly through live Hermes. | 2026-07-28 | Scorer (delegated) |
| — | Gate-crossing signal acceptance (change `gate-crossing-signal`) | **Deployed and LIVE (one operator step pending).** livedata `poll-gate` reads the spec-440 emission-gate state from finney via keyless JSON-RPC on pinned pre-verified storage keys at one finalized block; demand shares come from the TaoSwap panel over ALL non-root subnets (the chain's bar universe includes emission-disabled subnets); crossings are hysteresis-guarded (±10% of theta, 2-poll confirm) with lifecycle guards (first-seed silent, absence clears after 3 polls, gate-reactivation re-seeds). New Telegram class `gate-crossing` (instant tier, per-netuid 24h cooldown, suppressed-recorded-never-dropped). Off-device: 20 new livedata + 9 new telegram tests; full suites 68 + 78 green. **Endpoint decision: `https://entrypoint-finney.opentensor.ai`** (verified from the Pi). On the Pi 2026-07-28: first pass at block 8718574 seeded **128 sides silently (28 above / 100 below the bar), zero events** (correct seed behavior); observed theta 0.009273 (0.927%), q **0.75 explicit** (moved from the 0.61 code default by a root-origin path — the read-don't-assume design decision validated), h 3.0 `assumed-default` (storage null); `init` + `scan` quiet; `test --class gate-crossing` **delivered**. **Pending operator (sudo): install the updated service unit** (`sudo cp ~/atlas/repotrack/systemd/atlas-repotrack-update.service /etc/systemd/system/ && sudo systemctl daemon-reload`) to activate the hourly poll-gate line (chain-head poll → gate poll → Telegram scan). Calibration read ~2026-08-11: review recorded vs paged events, tune `hysteresis_pct`/`cooldown_hours` from evidence. Rollback: flip `gate_signal.enabled` + the class `enabled` false. | 2026-07-28 | Operator (delegated) + on-device runs |
| — | Network drift 440 → 443: rank-pinned gate bar + Root Reborn (chain evidence) | **Corpus re-synced 2026-08-06** (change `network-drift-443`; upstream `intoops-routines/references` edited first, then copied per the SOURCES.md procedure; new hashes, coverage date 2026-08-06; 23 units). Mainnet ran 441 (2026-08-03), 442 and 443 (both 2026-08-04); Atlas's `spec_upgrades` caught all three and paged, but the corpus did not know what changed. Chain evidence, each item validated against the merged code at tag **v443** AND a live Finney read on 2026-08-06: (1) **Emission gate bar is RANK-PINNED** — spec 441, PR #3014, `maybe_update_emission_gate_bar` now branches on `EmissionBarRank` (N): N > 0 pins theta to the Nth-largest **positive** demand share (fewer than N positive shares → theta falls to the smallest positive share and the gate stops biting); N = 0 is the old q-mass path. Code default at v443 is **32** (`DefaultEmissionBarRank`, chosen because q = 0.75 was crossing at ~rank 28, so the upgrade was tuned not to shift the curve) — **the v441 release notes say 64 and are WRONG, the code is authoritative**. Live read: `EmissionBarRank` unset → default 32 applies → **rank mode ACTIVE**, so the explicit `EmissionBarQuantile` of 0.75 that Atlas had been recording hourly is **inert**. Crossing detection was unaffected because Atlas reads theta from chain rather than recomputing it. A one-shot `migrate_reset_emission_gate_bar` cleared the stale quantile bar at upgrade. (2) **Root Reborn LIVE but curation-gated** — spec 441: root dividends reinvest into per-validator beta baskets under a global escrow coldkey, only root-registered hotkeys earn root dividends (remainder recycled), root unstakes hold-intervalled, calls 122/123 retired, `set_root_weights` moved to index 146, seed migration complete (`BetaBasketSeedInProgress` false). But `RootWeightSettingEnabled` is **false** on chain (ValueQuery bool, unset), so every fund runs the null strategy and dividends accumulate in place; the economically material event is a governance flip of `sudo_set_root_weight_setting_enabled`. 442/443 were hotfixes (#3035 ineligible root stake no longer diverts into alpha dividends; #3042 `stake_into_basket` gated pending state-growth review). (3) **Two shipped-signal defects found in the deployed store, from Atlas's own data.** The 441 bar reset dropped theta **14.5% in one poll** (0.008884 at 19:01 → 0.007598 at 20:07 → 0.007321 at 21:06) and at 21:06 subnets **49, 67, 79, 81** all recorded `rose-above` in a single pass with shares 0.0086–0.0094 — their demand never moved, the bar fell onto them, and all four paged as demand events. So a bar-parameter change produces exactly **\|M − N\|** simultaneous crossings against which the per-netuid cooldown is no defence, and the resulting alerts are affirmatively misleading. Separately, rank mode yields a **free correctness oracle**: exactly N positive shares sit at or above the bar, and `gate_sides` read exactly **32 above / 96 below** (28 seeded 2026-07-28 + 4 = 32), so Atlas's panel-derived shares independently reproduce the chain's rank-32 pin. Pinned keys derived as `twox128("SubtensorModule") ++ twox128(item)` and validated by reproducing the existing `EmissionGateBar` key byte-for-byte: `EmissionBarRank` `0x658faa385070e074c85bf6b568cf0555d33bd686290d014475513443305882be`, `RootWeightSettingEnabled` `0x658faa385070e074c85bf6b568cf055543f3dc4dfa03eb004921d663110b49b0`. Corpus rewritten (rank-pinning stated as current fact, not layered over the stale claim, so `supersession-markers.json` stays `[]`); benchmark extended with KB-EX-7/8, KB-PA-6 and adversarial **KB-AD-6** (the trap: a live-sounding q must be refused). | 2026-08-06 | Operator (delegated) + chain/code validation |
| — | Network-drift-443 deployment acceptance (change `network-drift-443`) | **Deployed and LIVE.** Pushed `c926ad8` (ships OFF), then enabled in two steps: `770529a` chain-parameter watch, `06bf403` the sixth Telegram class. Off-device 513 tests green (livedata 95, telegram 92, knowledge 48, repotrack 38, fleet 240); on the Pi under system Python 235 green (95/92/48 — note `pytest` is absent on the device, use `python3 -m unittest discover -s tests`). **10.2 verified:** poll at block 8783814 persisted `rank 32 assumed-default`, `bar_mode rank`, q 0.75 explicit, h 3.0 assumed-default, crossing detection unchanged. **10.3 DEFERRED, not skipped:** the above-bar invariant needs demand shares and the TaoSwap `/v2/subnets/` endpoint has been returning **HTTP 500 since 2026-08-06T03:34** (10 failures logged; a provider outage, unrelated to this change). The pass fails closed exactly as designed — `panel: live-unavailable`, `above_count` NULL, no write from stale data — and `gate_sides` still reads **32 above / 96 below** from the last good pass at 03:07, which is the invariant holding. Re-verify `above_count == 32` once TaoSwap recovers. **10.4:** watch enabled; all four items seeded (`EmissionBarRank` 32 assumed-default, `EmissionBarQuantile` 0.75 explicit, `EmissionGateExponent` 3.0 assumed-default, `RootWeightSettingEnabled` **false** assumed-default) with **zero transitions** across two passes — first-observation rule holds. **10.5:** `chain_param_events` was empty and the class had no watermark, so enabling could not backfill-flood; a blanket `init` was deliberately NOT run (it would have reseeded the six existing watermarks). Scan shows all seven adapters quiet; `test --class chain-parameter-change` **delivered**. **10.6:** corpus re-ingested (run `20260806T075140Z-b46ceeee`, 23 units, all `confirmed`) and activated; benchmark **`20260806T080300Z-8cd094e9` ACCEPTED** — correct-with-evidence 1.0, tool-call-rate 1.0, refusals-correct 1.0, fabrications 0 over the extended 30-exchange battery. KB-AD-6 (the trap) answered correctly: it reports the stored q of 0.75, states plainly that it does not set the bar, and calls the demand-mass-above-bar question not applicable under rank mode. One scorer classification was required (`KB-UN-2=refusal`): the answer refuses honestly ("the validated Atlas KB does not rank subnet teams by talent") but phrases it without a literal marker string. Rollback: `chain_params.enabled` and the class `enabled` are independent flags; neither gates crossing detection. | 2026-08-06 | Operator (delegated) + on-device runs |
| — | Corpus contradiction found by the post-archive doc audit + benchmark re-run | **Fixed and re-accepted.** Auditing the docs against the shipped code caught an internal contradiction the first re-sync missed: `fact-patterns.md` added rule **7d** ("the bar is no longer a q-mass quantile") while entries **3**, **3a** and **3b** still described the q-mass bar as CURRENT — 3a even gave `theta is the q-mass bar over demand shares` with the q = 0.61 default as live guidance. A corpus that contradicts itself is worse than one that is merely stale, since retrieval can return either side. Fixed upstream, re-snapshotted (`fact-patterns.md` → `29c1056cf907e23e`), re-ingested and activated on the Pi as run **`20260806T082006Z-66aacb36`** (23 units, all `confirmed`). Because the previously accepted benchmark had run against the contradictory corpus, that acceptance was stale and the battery was re-run: **`20260806T083003Z-58ae33ea` ACCEPTED** — correct-with-evidence 0.95, tool-call-rate 1.0, refusals-correct 1.0, fabrications 0. **Two scorer-sensitivity observations, deliberately NOT "fixed" by loosening the scorer:** (1) the single grounded miss is KB-HI-4, whose answer is substantively correct but wrote "Jun 2026" where the marker group expects the literal `june 2026` — a string-match artifact, and the run cleared the 0.9 bar without touching it; (2) three refusals (KB-UN-1, KB-UN-2, KB-AD-5) needed manual classification because the scorer's marker tuple lacks "cannot give" / "cannot answer" / "not answerable", though all three refuse explicitly and assert no live value. Both are LLM phrasing variance and will recur; widening the marker lists would make future runs auto-pass, so it is left as an operator judgement call rather than changed after seeing a miss. | 2026-08-06 | Operator (delegated) + on-device runs |

## Consequences already applied

- The formatted Pi means the ATLAS-ENV-002 removal plan is expected to be
  empty or trivial; the first inventory run now documents the *clean baseline*
  rather than a stale system. ATLAS-ENV-003 (hardening assessment) still
  applies in full to the fresh install.
- `var/` is gitignored: the Pi's checkout is the live working tree, and
  inventory reports (device-sensitive) must never be committed.

## Observations queued for the hardening assessment (ATLAS-ENV-003)

From inventory run `20260711T062206Z-5be02d8f` (clean baseline, no judgement
applied yet): no firewall tooling installed (ufw/nft/iptables all absent);
sshd config leaves `PasswordAuthentication` at its default (yes — operator
plans key-only after rotation); `X11Forwarding yes`; rpcbind listening on
0.0.0.0:111; mDNS (avahi) active. No Hermes, containers, or user software
present.

## Operational notes

- **Firewall/secret-file assessment needs a privileged inventory.** Reading the
  nftables ruleset and `/etc/ssl/private` requires root, so full hardening
  acceptance runs `sudo python3 inventory/atlas_inventory.py run` then
  `sudo python3 hardening/atlas_hardening.py assess`. These produce root-owned
  0600 outputs in `var/`; an unprivileged assessor cannot read a root-owned
  report (it fails closed rather than guessing). Routine, non-firewall checks
  remain fine unprivileged.

## Mining triage — chain findings (2026-08-07, change: mining-triage)

Verified on the Pi against live Finney and the TaoSwap panel. Recorded here
because each one corrects a belief that looked right and was not.

- **`alpha_out_emission` is the miner-facing quantity, and it is a
  constant.** 1.0 on all 127 non-root subnets. `alpha_in_emission` is the
  capped TAO-side pool injection, not income. Consequence: emission quantity
  carries no ranking information; price, owner capture and concentration do.
- **`MinerBurned` is measured, not set.** `run_coinbase.rs` computes it as
  the proportion of each tempo's miner incentive that landed on owner or
  owner-associated immune hotkeys and was burned or recycled. It withholds
  from the miner leg at distribution and penalises the subnet's price share
  next block. TaoSwap's `emission_miner_burn` matches chain `MinerBurned`
  on 127 of 128 subnets; one differs and is not yet explained.
- **Codec and key layout, both got wrong first.** `MinerBurned` is U96F32,
  divide by **2^32** — read at 2^64 it returns 0.0 for every subnet and
  looks clean. These netuid maps use the **Identity** hasher, so the key
  tail is the raw u16 with no twox64 concat — the wrong layout returns an
  empty map for every netuid. Both now carry regression tests, and the
  `twox128` derivation is self-tested against the three pinned gate keys.
- **Miner registration collateral is live in code and dormant on chain.**
  `CollateralLockShare` and `MinerCollateral` enumerate zero keys at
  finalized head, against a working control (`MinerBurned` 128 keys,
  `Owner` 300). Registration is fully burned today with no lockup. The
  mechanism shipped around spec 435, is settable to 95 percent of the
  registration price, and is recoverable only by earning emission
  ("a position that stops earning emission keeps its remaining collateral
  frozen indefinitely"). It is now in the chain-parameter watch as a
  netuid-keyed item, seeded silently, so a subnet enabling it emits a
  transition rather than quietly changing that subnet's entry risk.
- **The first VRAM parser matched nothing, and shipped.** Real subnet
  `min_compute.yml` files write `min_vram: 24` as a bare YAML number with
  the unit in a trailing comment, and split miner from validator. The
  original regex required an adjacent `GB`, so it extracted zero floors from
  all 34 indexed files while the board showed an empty hardware column. Now
  fixed: the miner section is scoped, `min_vram` is the floor (not
  `recommended_vram`, which is larger and is not a floor), the basis is
  recorded alongside the number, and an absent declaration stays `None`
  rather than becoming a fabricated zero. Verified against all 34 real
  files: 20 declare a floor, 14 declare none and every one of those genuinely
  has no VRAM key in its miner section.
- **Feasibility needed a scanner version.** Scans are sha-gated on the
  scanned commit, so a scanner-logic fix would never re-run against clones
  that had not moved and the old verdicts would sit there looking current.
  `SCAN_VERSION` in `atlas_fleet_mining.py` now invalidates every stored
  verdict when the scanner's logic changes. Bump it on any scanner change.
- **Field size is not competition.** `SubnetworkN` is 256 nearly everywhere
  while 3 to 15 UIDs earn anything and the top ten take substantially all
  of it. `active_miners` in the panel is the earner count less the owner's
  earning hotkey (verified exactly across eight subnets).

## Still open

**Gate calibration read (~2026-08-11) — carry these into it.** Three
findings from the 2026-08-06 drift work change what the read is looking at:

- **Events 1–26 predate bar-mode and `prev_theta` tracking.** They render
  without a mode or an attribution, and cannot be re-analysed for
  bar-versus-share cause. Only events from 2026-08-06 onward carry it.
- **The four events at 2026-08-03T21:06 (subnets 49, 67, 79, 81) are
  migration artefacts, not demand signals.** The spec-441 bar reset dropped
  theta 14.5% in one poll and those shares never moved. Exclude them when
  judging hysteresis, or they will argue for a wider band than reality
  needs. The re-seed guard now prevents this class of event entirely.
- **SN9's three crossings in ~30h were genuine share volatility**
  (0.0035 → 0.0192 → 0.0065 against a theta drifting only 0.0072 → 0.0079),
  so they are a real argument about `hysteresis_pct` / `cooldown_hours` —
  but note a 5.6x swing in an EMA-derived share within two hours is itself
  worth questioning before tuning the band around it.

**Deferred verification:** re-check the rank invariant (`above_count == 32`
against an effective N of 32). The TaoSwap `/v2/subnets/` outage that began
2026-08-06T03:34 **cleared by 2026-08-07T03:57** (HTTP 200, schema valid,
block 8789861), so this is unblocked.

**Mining triage, dated open items (2026-08-07):**

- **Budget band, due 2026-08-21.** The hardware and capital envelope was
  deliberately deferred to the data. `mining.budget_band` is null, which
  leaves rent unknown and the hardware cut rung inert. Pick a band within
  two weeks of the first board render. If it has not been picked by then,
  the honest conclusion is that mining is not being pursued and the screen
  should be disabled rather than maintained.
- **The parity assumption is a model.** The headline entrant figure shares
  the miner pool among earners + 1. It is labelled as a model on the board
  and in the spec. It still flatters a one-earner subnet, where one earner
  often means nobody else can compete rather than that a seat is open.
  Replacing it needs incentive-vector churn over time, which needs history
  the store does not yet have.
- **Registration cost units.** The panel reports a median of 0.006, low
  enough to question whether the field is TAO or alpha denominated. Confirm
  against a chain read of the same subnet's `Burn` before the value is
  presented as money.
- **One subnet of 128** where chain `MinerBurned` and the panel disagree.
  Identify it and decide whether it is tempo skew or a value to distrust.

**No Phase 0/1/2 blockers remain** — PRD §21 items 1–11 (and the
disk-threshold follow-up) were resolved 2026-07-11; items 12–15 (the corpus)
2026-07-12. Q18 (retrieval benchmark threshold) was effectively resolved by
the operator-approved threshold sheet of the accepted benchmark
(correct-with-evidence ≥ 0.9 etc.). Remaining later-phase questions
(16–17, 20+) stay open until their phase.

Standing reminders carried forward:

- **Backup restore test** (ATLAS-BACKUP-002): target is chosen (restic/rsync
  to a LAN machine, manual), but the backup is not accepted until a restore
  test succeeds — required before production acceptance.
- **ATLAS-HERMES-003 validation**: **fully closed 2026-07-12** — model
  validation run `20260711T182926Z-b1eef1c6` plus the retrieval benchmark
  run `20260711T192206Z-af3fa274` (deferred criterion closed; MV-RI-4
  re-test passed with retrieval available). A model change invalidates it.
- **Hermes baseline exceptions to close before production acceptance**
  (recorded in the acceptance entry): dedicated unprivileged service account
  (currently runs as `pi`), and a service unit for boot persistence
  (currently started manually).
- **PRD §21 Q19 (memory writes: keep approval-gated permanently?)**: the
  ATLAS-MEM-006 evidence base exists (run `20260711T171200Z-33321220`);
  writes remain approval-gated until the operator explicitly decides
  otherwise. Open operator decision, no phase blocks on it.
- **Conviction-activation conflict**: **CLOSED 2026-07-28.** The awaited
  enactment happened: mainnet jumped spec 424→432 at block 8636190
  (2026-07-16), carrying the conviction ownership-transfer enforcement
  (verified in the runtime code, called every epoch). The corpus was
  re-synced for real (chain-evidence entry above, 2026-07-28): the
  conviction section now states ACTIVE, the supersession marker is
  removed, and the same re-sync absorbed the July emission-model changes
  (root_prop out of the share at spec 432; Hill emission gate at spec
  440).
