{
  "_comment": "shinogi renderer (change: shinogi-renderer): composes the public shinogi.dev edition from stores already on disk, read-only, and publishes it into a second checkout when the content hash changes. No provider call, no model call, no chain call. Atlas writes shinogi; shinogi is never read back. Rollback: enabled false stops compose and publish and touches nothing else; publish false composes and writes nothing. Publishing enabled 2026-09-11: the Pi holds a write-scoped deploy key for vanlabs-dev/shinogi at ~/.ssh/id_ed25519_shinogi and the checkout at checkout_dir uses the SSH remote.",
  "enabled": true,
  "publish": true,
  "checkout_dir": "/home/pi/shinogi",
  "page_file": "index.html",
  "state_db": "var/shinogi/shinogi.db",
  "commit_name": "vanlabs-dev",
  "commit_email": "vanlabs@pm.me",
  "branch": "main",
  "window_hours": 6,
  "stale_hours": 26,
  "price_move_threshold_pct": 15,
  "share_move_threshold_pct": 25,
  "attention_rows": 10,
  "live_db": "var/livedata/livedata.db",
  "fleet_db": "var/fleet/fleet.db",
  "repo_db": "var/repotrack/repotrack.db",
  "knowledge_db": "var/knowledge/knowledge.db"
}
